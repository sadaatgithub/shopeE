import { ScrollView, StyleSheet, Text, View,StatusBar } from 'react-native'
import React from 'react'

const PodcastHome = () => {
  return (
    <ScrollView>
        <StatusBar hidden/>
      <Text>PodcastHome</Text>
    </ScrollView>
  )
}

export default PodcastHome

const styles = StyleSheet.create({})